model: Qwen/Qwen3-VL-30B-A3B-Instruct
label:
  zh_Hans: Qwen/Qwen3-VL-30B-A3B-Instruct
  en_US: Qwen/Qwen3-VL-30B-A3B-Instruct
model_type: llm
features:
- vision
model_properties:
  mode: chat
  context_size: 262144
parameter_rules:
- name: temperature
  use_template: temperature
  type: float
  default: 0.3
  min: 0.0
  max: 2.0
  help:
    zh_Hans: 用于控制随机性和多样性的程度。具体来说，temperature值控制了生成文本时对每个候选词的概率分布进行平滑的程度。较高的temperature值会降低概率分布的峰值，使得更多的低概率词被选择，生成结果更加多样化；而较低的temperature值则会增强概率分布的峰值，使得高概率词更容易被选择，生成结果更加确定。
    en_US: Used to control the degree of randomness and diversity. Specifically, the temperature value controls the degree to which the probability distribution of each candidate word is smoothed when generating text. A higher temperature value will reduce the peak value of the probability distribution, allowing more low-probability words to be selected, and the generated results will be more diverse; while a lower temperature value will enhance the peak value of the probability distribution, making it easier for high-probability words to be selected, the generated results are more certain.
- name: top_p
  use_template: top_p
  type: float
  default: 0.8
  min: 0.1
  max: 0.9
  help:
    zh_Hans: 生成过程中核采样方法概率阈值，例如，取值为0.8时，仅保留概率加起来大于等于0.8的最可能token的最小集合作为候选集。取值范围为（0,1.0)，取值越大，生成的随机性越高；取值越低，生成的确定性越高。
    en_US: The probability threshold of the kernel sampling method during the generation process. For example, when the value is 0.8, only the smallest set of the most likely tokens with a sum of probabilities greater than or equal to 0.8 is retained as the candidate set. The value range is (0,1.0). The larger the value, the higher the randomness generated; the lower the value, the higher the certainty generated.
- name: max_tokens
  use_template: max_tokens
  type: int
  default: 32768
  min: 1
  max: 131072
  help:
    zh_Hans: 用于指定模型在生成内容时token的最大数量，它定义了生成的上限，但不保证每次都会生成到这个数量。
    en_US: It is used to specify the maximum number of tokens when the model generates content. It defines the upper limit of generation, but does not guarantee that this number will be generated every time.
- name: top_k
  type: int
  help:
    zh_Hans: 返回概率最高的 k 个 token，取值范围是：[1, 100]，默认值为 50。
    en_US: 'Sample from the k most likely next tokens at each step. Lower k focuses
      on higher probability tokens. Range: [1, 100], default: 50.'
  required: false
  default: 50
  min: 1
  max: 100
- name: seed
  type: int
  help:
    zh_Hans: 生成时使用的随机数种子，用于控制模型生成内容的随机性。seed 支持无符号 64 位整数，默认值为 1234。
    en_US: 'The random seed used for generation to control the randomness of model
      generation. Supports unsigned 64-bit integers, default: 1234.'
  required: false
  default: 1234
  min: 1
  max: 2147483647
- name: repetition_penalty
  type: float
  help:
    zh_Hans: 用于控制模型生成时的重复度。提高该参数能降低模型生成的重复度，1.0表示不做惩罚。取值范围：[1.0, 2.0]，默认值为1.1。
    en_US: 'Controls repetition in model generation. Higher values reduce repetition,
      1.0 means no penalty. Range: [1.0, 2.0], default: 1.1.'
  required: false
  default: 1.1
  min: 1.0
  max: 2.0
- name: response_format
  type: string
  help:
    zh_Hans: 指定模型必须输出的格式，设置为 json_object 时，模型将输出有效的 JSON 对象。
    en_US: Specifies the format that the model must output. When set to json_object,
      the model will output a valid JSON object.
  required: false
  options:
  - text
  - json_object
pricing:
  input: '0.000'
  output: '0.000'
  unit: '0.000'
  currency: RMB