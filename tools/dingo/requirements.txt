# Dify plugin framework dependency
dify_plugin

# Core Dingo dependencies for plugin functionality
# Based on dingo/dingo-latest/requirements/runtime.txt

# Essential dependencies for text quality evaluation
chardet>=5.0.0
datasets>=2.0.0
jieba>=0.42.1
jsonlines>=3.0.0
pandas>=1.5.0
pydantic>=2.0.0
requests>=2.28.0
textstat>=0.7.0
numpy>=1.24.0

# Language and text processing
hanziconv>=0.3.2
langid>=1.1.6
nltk>=3.8
pyahocorasick>=2.0.0
wordninja>=2.0.0
zhon>=1.1.5

# Optional: Hugging Face integration (for dataset loading)
huggingface_hub>=0.16.0

# Optional: OpenAI integration (for LLM-based evaluation)
openai>=1.0.0

# Optional: Image processing (for multimodal support)
Pillow>=9.0.0
opencv-python>=4.5.0

# Utility packages
prettytable>=3.0.0
packaging>=21.0

# Note: The plugin will try to use local dingo installation first
# If not available, install with: pip install dingo-python
